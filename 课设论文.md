# 校园二手交易平台的设计与实现

## 摘要

随着高校信息化建设的不断推进，校园内的二手物品交易需求日益增长。传统的校园二手交易多依赖QQ群、贴吧、线下摆摊等方式，存在信息分散、交易效率低、缺乏信用保障等问题。本文设计并实现了一个基于 Flask 框架的校园二手交易平台，采用 B/S 架构，后端使用 Python Flask 框架，前端使用 Bootstrap 响应式设计，数据库采用 SQLite。系统实现了用户注册登录、商品发布与搜索、在线购买、商品留言、私信聊天、购物车、管理员后台等核心功能，为在校师生提供了一个安全、便捷、高效的二手物品交易渠道。

**关键词**：校园二手交易；Flask；Bootstrap；SQLite；电子商务

---

## 1 绪论

### 1.1 课题背景

高校校园是一个相对封闭的社区，每年新生入学、毕业生离校、学期更替都会产生大量的闲置物品流转需求。教材、电子产品、生活用品、运动器材等二手物品在校园内有巨大的交易市场。然而，目前大多数高校缺乏统一的二手交易平台，学生们主要依靠 QQ 群、微信群、校园贴吧、线下跳蚤市场等方式进行交易。这些方式存在以下问题：

（1）信息分散，查找不便。各类二手信息散落在不同群聊和帖子中，用户需要花费大量时间浏览和筛选。

（2）缺乏信用机制。交易双方缺乏身份认证，存在被欺诈的风险。

（3）交易流程不规范。没有标准化的商品展示、价格、交易状态管理等机制。

（4）沟通效率低。买家需要反复跟帖或私聊才能获取商品详情。

### 1.2 课题目的与意义

本课题旨在设计并开发一个专为高校校园服务的二手交易 Web 平台，实现二手物品的在线发布、浏览、搜索、购买、留言等功能。通过该平台，学生可以方便快捷地出售闲置物品或购买所需商品，提高闲置物品的利用率，促进校园资源的循环利用。

本课题的主要意义包括：

（1）**实用价值**：解决校园二手交易的实际痛点，提升交易效率和用户体验。

（2）**学习价值**：通过完整的项目开发流程，掌握 Web 应用开发的核心技术栈，包括 Flask 框架、ORM 数据库操作、用户认证、前后端交互等。

（3）**社会价值**：促进闲置物品的循环利用，符合绿色环保、可持续发展的理念。

### 1.3 论文结构

本文共分为七章。第一章介绍课题背景与意义；第二章进行需求分析；第三章阐述系统总体设计；第四章介绍数据库设计；第五章描述系统功能实现；第六章进行系统测试；第七章总结全文并展望未来工作。

---

## 2 需求分析

### 2.1 功能需求

经过对校园二手交易场景的分析，系统需要实现以下核心功能：

#### 2.1.1 用户管理模块

- **用户注册**：新用户填写用户名、手机号、密码等信息完成注册。
- **用户登录**：已注册用户通过用户名和密码登录系统。
- **退出登录**：用户可安全退出当前会话。
- **管理员账户**：系统预设管理员账户，拥有管理后台权限。

#### 2.1.2 商品管理模块

- **发布商品**：登录用户可发布商品，填写商品名称、分类、价格、描述，并可上传商品图片。
- **商品浏览**：所有用户均可浏览在售商品列表，支持分页展示。
- **商品搜索**：用户可通过关键词搜索商品名称或描述。
- **分类筛选**：用户可按照书籍、电子产品、生活用品、衣物、运动器材等分类筛选商品。
- **价格排序与区间筛选**：用户可按价格从低到高或从高到低排序，可设置价格区间。
- **商品详情**：查看商品的详细信息，包括图片、描述、价格、卖家信息等。

#### 2.1.3 交易模块

- **购买商品**：用户可将商品状态标记为"已售"，完成交易。
- **购物车**：用户可将感兴趣的商品加入购物车，便于集中查看和管理。

#### 2.1.4 互动模块

- **商品留言**：用户可在商品详情页发表留言，与卖家公开交流。
- **私信聊天**：用户之间可进行一对一的私信沟通，支持未读消息提示。

#### 2.1.5 管理后台

- **用户管理**：管理员可查看所有用户列表，删除违规用户。
- **商品管理**：管理员可查看所有商品列表，删除违规商品。

### 2.2 非功能需求

（1）**易用性**：界面简洁直观，操作流程清晰，新用户无需培训即可快速上手。
（2）**安全性**：用户密码采用哈希加密存储，表单提交启用 CSRF 保护。
（3）**响应式设计**：界面适配 PC 端和移动端不同屏幕尺寸。
（4）**可维护性**：代码结构清晰，遵循 MVC 分层架构，便于后续扩展和维护。

### 2.3 用例分析

系统涉及三类用户角色：

| 角色 | 权限描述 |
|------|----------|
| 游客 | 浏览商品、搜索商品、查看商品详情 |
| 注册用户 | 游客权限 + 发布商品、购买商品、商品留言、私信、购物车 |
| 管理员 | 注册用户权限 + 管理后台（用户管理、商品管理） |

---

## 3 系统总体设计

### 3.1 系统架构设计

本系统采用经典的 MVC（Model-View-Controller）三层架构：

- **Model 层（模型层）**：使用 SQLAlchemy ORM 框架定义数据模型，包括 User、Product、Message、PrivateMessage、CartItem 等实体类。
- **View 层（视图层）**：使用 Jinja2 模板引擎结合 Bootstrap 5 前端框架渲染 HTML 页面。
- **Controller 层（控制器层）**：使用 Flask 路由定义处理 HTTP 请求，实现业务逻辑。

系统架构如图所示：

```
浏览器 (Browser)
   ↓ HTTP 请求
Flask 路由层 (Routes / Controller)
   ↓ 调用
ORM 模型层 (Models)
   ↓ 操作
SQLite 数据库
```

返回方向：
```
SQLite 数据 → ORM 对象 → Jinja2 渲染 → HTML 响应 → 浏览器展示
```

### 3.2 技术选型

| 层次 | 技术 | 选型理由 |
|------|------|----------|
| 后端框架 | Python Flask 3.x | 轻量级、易上手、适合中小型项目 |
| 前端框架 | Bootstrap 5.3 | 成熟稳定、组件丰富、支持响应式 |
| 图标库 | Bootstrap Icons | 免费开源、与 Bootstrap 风格统一 |
| ORM | SQLAlchemy | Python 最流行的 ORM，简化数据库操作 |
| 数据库 | SQLite | 零配置、轻量级，适合课程项目 |
| 用户认证 | Flask-Login | 与 Flask 无缝集成，提供会话管理 |
| 表单处理 | Flask-WTF | 提供 CSRF 保护和表单验证 |
| 密码加密 | Werkzeug Security | Flask 内置的安全工具库 |

### 3.3 系统模块划分

系统功能模块如图：

```
校园二手交易平台
├── 欢迎首页
│   ├── 平台 Banner 展示
│   ├── 热门分类快捷入口
│   └── 最新商品展示
├── 用户模块
│   ├── 用户注册
│   ├── 用户登录
│   └── 退出登录
├── 商品模块
│   ├── 发布商品
│   ├── 商品浏览（搜索、分类、排序、价格区间）
│   ├── 商品详情
│   └── 我的发布
├── 交易模块
│   ├── 购买商品
│   └── 购物车管理
├── 互动模块
│   ├── 商品留言
│   └── 私信聊天
└── 管理模块
    ├── 用户管理
    └── 商品管理
```

---

## 4 数据库设计

### 4.1 E-R 图

系统主要实体包括：用户（User）、商品（Product）、留言（Message）、私信（PrivateMessage）、购物车项（CartItem）。

实体关系简要描述：

- 用户与商品：一对多关系（一个用户可以发布多个商品）。
- 用户与留言：一对多关系（一个用户可以发表多条留言）。
- 商品与留言：一对多关系（一个商品可以有多条留言）。
- 用户与私信：用户作为发送者和接收者，多对多关系。
- 用户与购物车：一对多关系（一个用户可以有多个购物车项）。
- 购物车项与商品：多对一关系（每个购物车项关联一个商品）。

### 4.2 数据表设计

#### 4.2.1 用户表（users）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | Integer | 主键，自增 | 用户唯一标识 |
| username | String(50) | 唯一，非空 | 用户名 |
| password_hash | String(256) | 非空 | 密码哈希值 |
| phone | String(20) | 非空 | 手机号 |
| is_admin | Boolean | 默认 False | 是否为管理员 |
| created_at | DateTime | 默认当前时间 | 注册时间 |

#### 4.2.2 商品表（products）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | Integer | 主键，自增 | 商品唯一标识 |
| title | String(100) | 非空 | 商品名称 |
| description | Text | 非空 | 商品描述 |
| price | Float | 非空 | 商品价格 |
| category | String(30) | 非空 | 商品分类 |
| image | String(200) | 默认 default.jpg | 商品图片路径 |
| status | String(10) | 默认"在售" | 商品状态（在售/已售） |
| seller_id | Integer | 外键→users.id，非空 | 卖家ID |
| created_at | DateTime | 默认当前时间 | 发布时间 |

#### 4.2.3 留言表（messages）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | Integer | 主键，自增 | 留言唯一标识 |
| content | Text | 非空 | 留言内容 |
| product_id | Integer | 外键→products.id，非空 | 所属商品ID |
| user_id | Integer | 外键→users.id，非空 | 留言用户ID |
| created_at | DateTime | 默认当前时间 | 留言时间 |

#### 4.2.4 私信表（private_messages）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | Integer | 主键，自增 | 私信唯一标识 |
| content | Text | 非空 | 私信内容 |
| sender_id | Integer | 外键→users.id，非空 | 发送者ID |
| receiver_id | Integer | 外键→users.id，非空 | 接收者ID |
| is_read | Boolean | 默认 False | 是否已读 |
| created_at | DateTime | 默认当前时间 | 发送时间 |

#### 4.2.5 购物车表（cart_items）

| 字段名 | 数据类型 | 约束 | 说明 |
|--------|----------|------|------|
| id | Integer | 主键，自增 | 购物车项唯一标识 |
| user_id | Integer | 外键→users.id，非空 | 用户ID |
| product_id | Integer | 外键→products.id，非空 | 商品ID |
| created_at | DateTime | 默认当前时间 | 添加时间 |

---

## 5 系统实现

### 5.1 项目结构

```text
campus-trade/
├── app.py                  # Flask 主程序（路由定义）
├── models.py               # 数据库模型定义
├── forms.py                # 表单验证类
├── requirements.txt        # Python 依赖列表
├── instance/
│   └── trade.db            # SQLite 数据库文件
├── static/
│   └── uploads/            # 商品图片上传目录
└── templates/              # Jinja2 模板目录
    ├── base.html           # 基础布局模板
    ├── home.html           # 欢迎首页
    ├── browse.html         # 商品浏览页
    ├── login.html          # 登录页
    ├── register.html       # 注册页
    ├── publish.html        # 发布商品页
    ├── product_detail.html # 商品详情页
    ├── cart.html           # 购物车页
    ├── messages.html       # 私信列表页
    ├── conversation.html   # 私信对话页
    ├── my_products.html    # 我的发布页
    └── admin.html          # 管理后台页
```

### 5.2 核心功能实现

#### 5.2.1 用户认证

系统使用 Flask-Login 扩展实现用户会话管理。用户密码通过 Werkzeug 的 `generate_password_hash()` 函数采用安全的哈希算法加密存储，登录时使用 `check_password_hash()` 进行验证。关键代码如下：

```python
from werkzeug.security import generate_password_hash, check_password_hash

class User(UserMixin, db.Model):
    # ...
    def set_password(self, password):
        self.password_hash = generate_password_hash(password)

    def check_password(self, password):
        return check_password_hash(self.password_hash, password)
```

#### 5.2.2 商品搜索与筛选

商品浏览功能支持多条件组合查询，包括关键词模糊搜索、分类精确筛选、价格区间筛选、价格排序。使用 SQLAlchemy 链式调用构建动态查询：

```python
query = Product.query.filter_by(status="在售")
if keyword:
    query = query.filter(
        Product.title.contains(keyword) | Product.description.contains(keyword)
    )
if category:
    query = query.filter_by(category=category)
if sort == "price_asc":
    query = query.order_by(Product.price.asc())
elif sort == "price_desc":
    query = query.order_by(Product.price.desc())
else:
    query = query.order_by(Product.created_at.desc())
```

#### 5.2.3 私信功能

私信系统支持用户间一对一实时消息传递。系统维护 `private_messages` 表记录所有消息，通过 `is_read` 字段区分已读和未读状态。导航栏实时显示未读消息数量。关键实现包括：

- 私信列表按最近联系时间排序
- 进入对话后自动将对方消息标记为已读
- 聊天界面使用 CSS 气泡样式区分发送和接收消息

#### 5.2.4 购物车

购物车功能允许用户将感兴趣的商品暂存，集中管理待购物品。每个用户维护独立的购物车列表。当商品被购买（状态变为"已售"）时，系统自动从所有用户的购物车中移除此商品。

### 5.3 首页设计

系统分设欢迎首页和市场浏览两个独立页面。欢迎首页展示平台 Banner、热门分类入口、最新商品推荐和使用流程引导；市场浏览页提供完整的商品搜索、筛选、排序功能和商品卡片展示。两个页面通过导航栏的"首页"和 Hero 区域的"立即逛逛"按钮进行导航。

### 5.4 响应式设计

前端使用 Bootstrap 5 的栅格系统实现响应式布局。商品卡片在 PC 端显示为 4 列（`row-cols-lg-4`），平板端 2 列（`row-cols-md-2`），手机端 1 列（`row-cols-1`），确保在不同屏幕尺寸下都能获得良好的浏览体验。

---

## 6 系统测试

### 6.1 测试环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 11 |
| Python 版本 | 3.12 |
| 浏览器 | Chrome（最新版） |
| 数据库 | SQLite 3 |

### 6.2 功能测试用例

#### 6.2.1 用户注册与登录

| 测试项 | 操作步骤 | 预期结果 | 测试结果 |
|--------|----------|----------|----------|
| 正常注册 | 填写用户名、手机号、密码，点击注册 | 提示"注册成功"，跳转登录页 | 通过 |
| 重复注册 | 使用已存在的用户名注册 | 提示"用户名已存在" | 通过 |
| 密码不一致 | 两次密码输入不同 | 提示"两次密码不一致" | 通过 |
| 正常登录 | 输入正确的用户名和密码 | 提示"登录成功"，跳转首页 | 通过 |
| 错误登录 | 输入错误的密码 | 提示"用户名或密码错误" | 通过 |

#### 6.2.2 商品发布与浏览

| 测试项 | 操作步骤 | 预期结果 | 测试结果 |
|--------|----------|----------|----------|
| 发布商品 | 填写商品信息并提交 | 提示"发布成功"，商品出现在列表 | 通过 |
| 关键词搜索 | 输入关键词搜索 | 返回匹配的商品 | 通过 |
| 分类筛选 | 选择"书籍"分类 | 仅显示书籍类商品 | 通过 |
| 价格排序 | 选择"价格从低到高" | 商品按价格升序排列 | 通过 |
| 价格区间 | 设置最低价和最高价 | 仅显示区间内商品 | 通过 |

#### 6.2.3 购买与购物车

| 测试项 | 操作步骤 | 预期结果 | 测试结果 |
|--------|----------|----------|----------|
| 立即购买 | 点击"立即购买" | 商品状态变为"已售" | 通过 |
| 加入购物车 | 点击"加入购物车" | 提示"已加入购物车" | 通过 |
| 移除购物车 | 点击"移除" | 商品从购物车中删除 | 通过 |
| 不能自购 | 尝试购买自己发布的商品 | 提示"不能购买自己的商品" | 通过 |

#### 6.2.4 留言与私信

| 测试项 | 操作步骤 | 预期结果 | 测试结果 |
|--------|----------|----------|----------|
| 发表留言 | 在商品详情页输入留言 | 留言出现在商品留言区 | 通过 |
| 发送私信 | 通过"联系卖家"进入私信对话 | 消息成功发送，对方可见 | 通过 |
| 未读提示 | 收到新私信 | 导航栏显示未读数量角标 | 通过 |

#### 6.2.5 管理后台

| 测试项 | 操作步骤 | 预期结果 | 测试结果 |
|--------|----------|----------|----------|
| 管理员登录 | 使用 admin 账户登录 | 导航栏显示"管理后台"入口 | 通过 |
| 用户管理 | 查看用户列表，删除普通用户 | 用户被删除，相关数据清理 | 通过 |
| 商品管理 | 查看商品列表，删除违规商品 | 商品被删除，相关留言清理 | 通过 |
| 权限控制 | 普通用户访问 /admin | 提示"无权访问"，跳转首页 | 通过 |

### 6.3 测试结论

通过上述功能测试，系统的各项核心功能均能正常运行。用户注册登录流程完整，商品发布浏览搜索功能正常，交易与购物车逻辑正确，留言私信通信流畅，管理后台权限控制有效。系统达到了预期的设计目标。

---

## 7 总结与展望

### 7.1 工作总结

本文设计并实现了一个基于 Flask + Bootstrap + SQLite 的校园二手交易平台。主要完成了以下工作：

（1）深入分析了校园二手交易的实际需求，明确了系统的功能需求和非功能需求。

（2）设计了系统的三层架构，选择了适合课程项目规模的技术栈。

（3）设计了完整的数据库模型，包含用户、商品、留言、私信、购物车五张数据表。

（4）实现了用户注册登录、商品发布浏览搜索、在线购买、购物车管理、商品留言、私信聊天、管理员后台等完整功能。

（5）设计了响应式的前端界面，在 PC 端和移动端均有良好的用户体验。

（6）进行了全面的功能测试，验证了系统的正确性和稳定性。

### 7.2 不足与展望

由于时间和能力有限，本系统仍存在以下可以改进的地方：

（1）**支付功能**：目前交易仅通过状态标记实现，未接入实际支付系统。后续可接入支付宝、微信支付等第三方支付接口。

（2）**订单管理**：缺少完整的订单跟踪流程，包括发货确认、收货确认、退换货等功能。

（3）**用户评价**：交易完成后缺少评价机制，无法建立信用体系。

（4）**消息通知**：目前仅在页面刷新时更新未读消息数，可引入 WebSocket 实现实时推送通知。

（5）**图片管理**：图片上传缺少压缩和裁剪功能，大图片可能影响加载速度。

（6）**数据统计与分析**：可以为卖家提供商品浏览量、收藏量等数据分析功能。

---

## 参考文献

[1] Miguel Grinberg. Flask Web 开发：基于 Python 的 Web 应用开发实战[M]. 安道, 译. 人民邮电出版社, 2018.

[2] Bootstrap 团队. Bootstrap 5.3 官方文档[EB/OL]. https://getbootstrap.com/docs/5.3/, 2024.

[3] SQLAlchemy 团队. SQLAlchemy 2.0 Documentation[EB/OL]. https://docs.sqlalchemy.org/en/20/, 2024.

[4] Flask 团队. Flask 3.1 Documentation[EB/OL]. https://flask.palletsprojects.com/en/3.1.x/, 2024.

[5] Eric Matthes. Python编程：从入门到实践[M]. 3版. 袁国忠, 译. 人民邮电出版社, 2023.

[6] Ian Sommerville. 软件工程[M]. 10版. 彭鑫, 赵文耘, 等译. 机械工业出版社, 2018.

[7] 王珊, 萨师煊. 数据库系统概论[M]. 5版. 高等教育出版社, 2014.
